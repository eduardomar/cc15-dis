import IOption from './IOption';

const options: IOption[] = [
  { value: 'DocumentSubmission' },
  { value: 'DocumentValidationResponse' },
  { value: 'DocumentReviewResponse' },
];

export default options;
