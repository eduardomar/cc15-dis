import IOption from './IOption';

const options: IOption[] = [
  { value: 'SINGLE_TXN', label: 'Single Transaction' },
  { value: 'CONTINUOUS', label: 'Continuous Transaction' },
];

export default options;
